[project]
name = 'pipe-hdb-spark'
version = '0.1.0'

requires-python = '==3.12.*'

dependencies = [
    'pyspark==4.0.1',
    'polars',
    'requests',
    'pandas',
    'pyarrow',
    'plotly',
]



[dependency-groups]
dev = [
    "ipython"
]

